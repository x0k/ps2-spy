#!/bin/bash -e

run:
    BOT_DISCORD_TOKEN=$(cat ~/Sync/ps2-spy/test-token) go run cmd/ps2-spy/*.go --config config/local.yml

all:

test:
    go test ./...

o:
    go run tools/explorer/*.go --tag $1 --output tmp
