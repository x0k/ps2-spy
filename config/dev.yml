storage:
  path: "sqlite://storage/storage.db" # required
  migrations_path: "file://db/migrations" # required
logger:
  level: debug
  handler_type: pretty
profiler:
  enabled: true
  address: 0.0.0.0:6060
metrics:
  enabled: true
  address: 0.0.0.0:9099
census:
  # service_id: required
  streaming_endpoint: wss://push.nanite-systems.net/streaming
discord:
  # token: required